packages:
  - apps/*
  - packages/*

# pnpm ≥10 blocks dependency lifecycle scripts unless allowlisted.
# esbuild (pulled in by vite) needs its postinstall to place the platform binary.
onlyBuiltDependencies:
  - esbuild
